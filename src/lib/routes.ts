import type { Route } from "next";

export const ROUTES = {
  HOME: "/" as Route,
  BLOG: "/blog" as Route,
  COOKIE_POLICY: "/cookie-policy" as Route,
  SIGN_IN: "/sign-in" as Route,
  SIGN_UP: "/sign-up" as Route,
  APP: "/app" as Route
};
